---
- name: Disable Windows user accounts on Hyper-V VM
  hosts: {{ hostname }}
  gather_facts: no
  tasks:
    - name: Disable user accounts
      win_user:
        name: "{{ item }}"
        state: disabled
      loop: "{{ users_to_disable }}"
