---
- name: Disable Windows user accounts on Hyper-V VM
  hosts: all
  gather_facts: no
  tasks:
  - name: Disable user account
    win_shell: | 
      Get-LocalUser | Where-Object {$_.Name -ne "administrator" -or $_.Name -ne "blueAdmin" -or $_.Name -ne "ThePatchelist"} | Disable-LocalUser
